name: Lint and Type Check

on:
  workflow_dispatch:

  push:
    paths:
      - "src/**"
    branches:
      - main
      - develop

  pull_request:
    types: [opened, synchronize]
    paths:
      - "src/**"
    branches:
      - main
      - develop

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      # Checkout
      - name: Checkout
        uses: actions/checkout@v6

      # Setup tools
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Deno Setup
        uses: denoland/setup-deno@v2
        with:
          deno-version: latest

      # Install deps
      - name: Install dependencies
        run: bun install --frozen-lockfile

      # Run checks (Deno lint, Biome/Bun lint, Type Check)
      - name: Deno Lint
        run: deno lint

      - name: Bun Lint (Biome)
        run: bun lint

      - name: Type Check
        run: bun typecheck
